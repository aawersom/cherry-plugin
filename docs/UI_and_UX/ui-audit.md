# Cherry — UI/UX аудит вёрстки (2026-06-04)

Комплексный аудит после миграции на `Lampa.InteractionCategory`. Источники:
- Self-audit вёрстки Cherry (агент ui-designer по `plugin.js`)
- Сравнение с рабочими референсами `sisi_full.js` («Клубничка») и `adultjs.js` (AdultJS)
- Гайдлайны скилла `ui-ux-pro-max` (адаптированы под 10-foot TV / D-pad)

---

## Главная находка (root cause)

Миграция на `InteractionCategory` **осиротила весь кастомный слой вёрстки**:
- 6 шаблонов (`cherry_card`, `cherry_source_card`, `cherry_group_label`, `cherry_source_row`, + тела `cherry_main`/`cherry_grid`) — **не используются** (InteractionCategory строит свой DOM из стокового `.card`).
- ~300–390 строк CSS (`.cherry-card*`, `.cherry-grid*`, `.cherry-source-*`, `@keyframes cherry-spin`) — **мёртвый код**.
- Живут **только 2 правила**: `.cherry-cat .card__view` (16:9) и `.cherry-cat .card__img` (cover).

**Следствие (на момент аудита):** весь задуманный фокус-глоу, бейдж длительности, обрезка заголовка, индикатор избранного, кастомный спиннер/empty — отсутствовали; юзер видел сток Lampa + 2 правила.

> **Разрешено (2026-06-04).** Мёртвый CSS/шаблоны удалены, а нужная полировка
> переделана на `.cherry-cat`-скоуп поверх стокового `.card`: 16:9, focus `scale(1.04)`,
> 2-строчный белый заголовок, HD/длительность в quality-слоте, `.cherry-src-badge`,
> кастомный `empty(msg)`. Статусы по каждому пункту — в таблицах ниже.

**Важно (валидация):** sisi и AdultJS используют **тот же подход** — стоковый `.card` + InteractionCategory. То есть наш переезд правильный. Более того, **наш 16:9 лучше** референсов: они льют горизонтальные превью в постер 2:3 (crop), а мы сделали честный 16:9.

---

## Приоритизированный план улучшений

> **Статус внедрения (2026-06-04).** Помечен каждый пункт. Большинство P0/P2/P3 сделано;
> P1 (dwell-превью) пропущен по решению владельца; HD-бейдж сделан; global search — TODO.

### P0 — Тех.долг (по принципам владельца — обязательно)

| # | Что | Где | Статус |
|---|---|---|---|
| 0.1 | Удалить ~390 строк мёртвого CSS + 6 неиспользуемых шаблонов | `addStyles()` (теперь `plugin.js:1071`, ~30 строк), шаблоны | ✅ **Implemented** — CSS урезан до `.cherry-cat`-скоупа, все 6 `Lampa.Template.add` удалены, `addTemplates()` нет |

### P1 — Высокий эффект, дёшево (1–3 строки каждое)

| # | Что | Где | Статус |
|---|---|---|---|
| 1.1 | **Ambient backdrop** — `v.background_image = v.thumb` | `toCard()` `plugin.js:434` | ⏸ **Deferred** — `toCard` не выставляет `background_image` (только img/poster/quality/source) |
| 1.2 | **Dwell-таймер превью 1.5с + crossfade** вместо мгновенного play | `_startPreview`/`onFocus` `plugin.js:565`, `926` | ⏸ **Deferred (по решению владельца)** — превью играет сразу на фокусе, без dwell/fade |

### P2 — Видимая полировка (нужно твоё решение по вкусу)

| # | Что | Где | Статус |
|---|---|---|---|
| 2.1 | **Фокус карточки** — сток Lampa ИЛИ брендовый ring | `addStyles` `plugin.js:1088` | ✅ **Implemented (вариант без ring)** — оставлен один нативный фрейм + `transform:scale(1.04)`; розовый ring сознательно НЕ добавлен (стекался бы поверх нативного = двойная рамка) |
| 2.2 | **Заголовок 2 строки + полный белый** | `.cherry-cat .card__title` `plugin.js:1095` | ✅ **Implemented** — `-webkit-line-clamp:2; color:#fff; font-size:.9em` |
| 2.3 | **Главная-пикер**: буквенные плитки + отличить Поиск/Избранное как действия | `CherryMain.create/cardRender` `plugin.js:976`, `1007` | ✅ **Implemented** — `.cherry-tile` с буквой, action-плитки с брендовым тоном (`--action`), источники со стабильным hue (`_tileColor`) |
| 2.4 | Проверить **cols--5 vs cols--4** на реальном ТВ | `cols--5` `plugin.js:763`; home `cols--8` `plugin.js:1003` | ⏸ **Deferred** — оставлено `cols--5` (грид) / `cols--8` (пикер); проверка на устройстве за владельцем |

### P3 — Функциональные улучшения (больше работы)

| # | Что | Где | Статус |
|---|---|---|---|
| 3.1 | **Активный фильтр в заголовке** (сорт/категория суффиксом) | `_filteredTitle`/`_pushFiltered` `plugin.js:612`, `621` | ✅ **Implemented** — фильтр уходит в title активити («Pornhub · MILF · Свежее»), виден после закрытия меню |
| 3.2 | **HD/4K бейдж** в `quality` (объединить с длительностью) | `toCard` `plugin.js:439` + адаптеры | ✅ **Implemented** — `v.hd` едет в `quality`-слот вместе с длительностью; заполнен xvideos (`video-hd-mark`), youjizz (`i-hd`) |
| 3.3 | **Error ≠ empty** + постоянная подсказка пустого избранного | `empty(msg)` `plugin.js:791`, `create` `plugin.js:751`/`766` | ✅ **Implemented** — `cherry_load_error` ≠ `cherry_no_results`; пустое избранное = постоянный `cherry_fav_empty_hint` |
| 3.4 | **Атрибуция источника** в поиске по всем (бейдж сайта) | `cardRender` `plugin.js:824` | ✅ **Implemented** — `.cherry-src-badge` на карточках в all_sources И избранном |
| 3.5 | **Lampa global search** регистрация + кнопка фильтра в шапке | `addFilterButton` `plugin.js:1168` | ⚠ **Частично** — кнопка фильтра в шапке ✅; регистрация в системный global search ⏸ **TODO** (API не подтверждён на живом рантайме, см. комментарий в коде) |

### P4 — Опционально, крупное

| # | Что | Статус |
|---|---|---|
| 4.1 | **Главная как InteractionMain (ряды по источникам)** в стиле Netflix | ⏸ **Deferred** — главная осталась тайл-пикером; ряд-режим (`cherry_home_mode`) был добавлен в UX-v2, но удалён при переезде на InteractionCategory |

---

## Сводная таблица (Cherry vs референсы)

| Фича | sisi | AdultJS | Cherry сейчас | Статус |
|---|---|---|---|---|
| Разметка карточки | сток `.card` | сток `.card` | сток `.card` (`.cherry-cat`) | ✅ мёртвый CSS/шаблоны удалены |
| Аспект | постер 2:3 | 2:3 | **16:9** | ✅ держим (лучше реф.) |
| В ряду | cols--3 | cols--3 | cols--5 (пикер cols--8) | ⏸ проверка плотности на ТВ |
| Фокус | дефолт | дефолт | нативный фрейм + `scale(1.04)` | ✅ без кастомного ring (сознательно) |
| Длительность | quality=time | quality=dur | quality=secToTime | ✅ готово |
| HD/4K | нет | **да** | `v.hd` в quality-слоте | ✅ добавлено |
| Ambient backdrop | да | да | нет | ⏸ deferred |
| Превью на фокусе | 1.5с + fade | да | есть, без dwell/fade | ⏸ deferred (решение владельца) |
| Главная | ряды (Main) | ряды | грид-пикер (буквенные плитки) | ✅ пикер с цветными тайлами |
| Фильтр UI | onRight+шапка+поиск | то же | onRight + кнопка в шапке | ✅ (global search — TODO) |
| Активный фильтр | показан | показан | в title активити | ✅ показан после закрытия меню |
| Атрибуция источника | — | — | `.cherry-src-badge` | ✅ в all_sources + избранном |
| Error ≠ empty | да | — | `empty(msg)` | ✅ ошибка/пусто/избранное-пусто различимы |

---

## Рекомендуемый порядок внедрения — итог

1. **P0** (удалить мёртвый код) — ✅ сделано.
2. **P1** (ambient backdrop + dwell-превью) — ⏸ deferred (dwell-превью пропущен по решению владельца; backdrop не делали).
3. **P2** (фокус + заголовок + главная-пикер) — ✅ сделано (фокус без ring — сознательно).
4. **P3** (фильтр в заголовке, error-state, атрибуция) — ✅ сделано; **global search** — ⏸ TODO.
5. **P4** (главная рядами) — ⏸ deferred.
