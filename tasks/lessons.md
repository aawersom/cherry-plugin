
## 2026-09-05 — регрессия после партии каналов (владелец: pornhub «не открывается», навигация в избранном)
- Матрица `tv-audit6` проверяет адаптеры (карточки/поиск/стрим), но НЕ поведение реального грида: фокус, контроллер, повторная сборка. Регрессию избранного (v0.13.17: `Sync.run()` при открытии → старый `_refreshGrid()` → `comp.create()` поверх построенного грида) она пропустила, хотя избранное «проверялось» — по данным, не по клавишам.
- Правило: после любого изменения, затрагивающего жизненный цикл грида (load/resolve/sync/empty-state), прогонять реальный грид с adb-клавишами: `tv-eval.mjs test/tv-grid-open.page.js "<id|favorites>:9000"` (без инъекции — как в проде) → `tv-state.page.js` после DPAD (фокус должен сдвигаться по индексам). Для избранного — с реальным бакетом через PIN на стенде.
- Второй урок: «не открывается» на ТВ владельца ≠ «не открывается» на стенде (EE-IP). Прежде чем лечить, снимать факты с устройства: тайминги по маршрутам (`tv-ph-timing.page.js`: pornhub натив 4.0 с vs прокси 0.7 с) и спрашивать точный симптом (спиннер / пусто / ошибка).

## 2026-09-05 — «каждый канал должен играть» (v0.13.25)
- «Открывается» ≠ «играет». `tv-audit6` подтверждал стрим по HTTP-статусу; реальный hls.js получал 410/470 и молча умирал. Проверять только штатным `playVideo` на стенде (`tv-verify-play.page.js`), для HLS — по `currentTime`/FRAG_LOADED, для MP4 — Range-фетч с Referer страницы, как делает внешний плеер. Форсировать inner для MP4-каналов нельзя: «Format error» — артефакт харнесса, не бага.
- Lampa-плеер берёт ВЫСШИЙ пункт карты `quality` и игнорирует `url` адаптера (при пустой `video_quality_default`). Смотреть в харнесе `handedAtCall` vs `handed`; менять дефолт качества — только фильтрацией карты (youjizz ≤720p, pornve без AV1-1080p).
- Токен-привязки — сначала матрица (IP / UA / Referer / ASN) curl-ом с трёх точек выхода (стенд, VPS, CF), потом код. У pornhub оказалось три фактора сразу плюс мёртвая схема подписи в 2/3 рендеров — без матрицы каждый фикс лечил один фактор и «не работало».
- Мёртвая инфраструктура молчит: residential-пул воркера умер без ошибок в коде, wrangler OAuth истёк 2026-06-17. Раз в релиз — прогон `tv-verify-play` по всем каналам, а не только по тронутым.
